import styled from "@emotion/styled";

const Main = styled.div(({ theme }) => ({
    backgroundColor : "#cbcbd226",
    position: "relative",
    // float: "right",
    // width: "calc(100% - 260px)",
}));

export default Main